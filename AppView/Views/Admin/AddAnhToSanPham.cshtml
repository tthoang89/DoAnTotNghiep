@model List<AppData.Models.MauSac>

@{
    ViewData["Title"] = "Thêm ảnh";
}

<h1>AddAnhToMauSac</h1>

<form enctype="multipart/form-data" asp-action="AddAnhToSanPham">
    <table class="table">
        <thead>
            <tr>
                <th>
                    Chọn ảnh
                </th>
                <th>
                    Tên màu
                </th>
                <th>
                    Mã Màu
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @for(var i=0;i<Model.Count;i++)
            {
                <tr>
                    <td>
                        <input type="file" name="images" />
                    </td>
                    <td>
                       @Html.DisplayFor(modelItem=>Model[i].Ten)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem=>Model[i].Ma)
                    </td>
                    <td>
                        <input type="text" name="maMaus" value="@Model[i].Ma" />
                    </td>
                    @*                     <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                </td> *@
                </tr>
            }
        </tbody>
    </table>
    <button class="btn btn-primary" type="submit">Submit</button>
</form>
