@model IEnumerable<AppData.ViewModels.SanPham.AnhRequest>

@{
    ViewData["Title"] = "AddAnhToMauSac";
}

<h1>AddAnhToMauSac</h1>

<form enctype="multipart/form-data" asp-action="AddAnhToMauSac">
    <table class="table">
        <thead>
            <tr>
                <th>
                    Chọn ảnh
                </th>
                <th>
@*                     @Html.DisplayNameFor(model => model.TenMauSac)
 *@                </th>
                <th>
@*                     @Html.DisplayNameFor(model => model.MaMauSac)
 *@                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        <input type="file" name="images" />
                    </td>
                    <td>
@*                         @Html.DisplayFor(modelItem => item.TenMauSac)
 *@                    </td>
                    <td>
@*                         @Html.DisplayFor(modelItem => item.MaMauSac)
 *@                    </td>
                    <td>
@*                         <input type="text" name="idMauSacs" value="@item.IDMauSac"/>
 *@                    </td>
@*                     <td>
                        @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                        @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                        @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                    </td> *@
                </tr>
            }
        </tbody>
    </table>
    <button class="btn btn-primary" type="submit">Submit</button>
</form>

